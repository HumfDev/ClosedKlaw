# Website server (copy to website/.env and fill in)

PORT=3000

# Supabase — Project Settings → API
# When both are set, waitlist signups save to Supabase instead of waitlist.db
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
