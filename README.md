# ClosedKlaw

iMessage agent and marketing site for automated job-application workflows.

## Structure

- `agent/` — Mac iMessage watcher, OAuth connectors, and apply workers
- `website/` — Landing page, waitlist, and terms
- `docs/` — Setup guides (e.g. Google OAuth)
